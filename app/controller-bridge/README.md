# Gaia Controller Bridge Scaffold

This folder hosts bridge routes used by the iPad controller shell.

Planned routes:

- `presentation/route.swift`
- `navigation/route.swift`
- `slides/route.swift`

Bridge responsibilities:

- Keep Aither service credentials server-side.
- Expose controller-safe manifest and navigation endpoints.
- Map upstream errors to actionable bridge-level responses.
